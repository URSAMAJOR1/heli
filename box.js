class Box{} 
    var options={
        restiution:1 ,

    }
    this.body=Bodie.rectangle(200,100,50,50)
    World.add(world.this.body);
    display()
       var pos =this.body.position;
        recMode(CENTER)
        Fill(255)
    rect(pos.x,pos.y,this.width,this.height)
    
